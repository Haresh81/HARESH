#include<stdio.h>
#define n 5
int minimam(int a[],i)
{
	int i,t;
	for(i=0;i<n;i++)
	{
		if(a[i]>i)
		t=
	}
	return i;
}
int maximam1(int a[])
{
	int i,t;
	for(i=0;i<n;i++)
	{
		if(a[i]<i)
		t=
	}

int main()
{
	int i,max,mini;
	for(i=0;i<n;i++)
	{
		printf("enter a[%d]:",max);
		scanf("%d",&max);
		printf("enter a[%d]:",mani);
		scanf("%d",&mini);
	}
	minimam(a);
}